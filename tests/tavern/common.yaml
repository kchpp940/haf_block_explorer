---
  name: Common test values
  description: Common values for tests

  variables:
    service:
      proto: http
      server: "{tavern.env_vars.HAFBE_ADDRESS}"
      port: "{tavern.env_vars.HAFBE_PORT}"
      base_path: "{tavern.env_vars.HAFBE_API_BASE_PATH:/hafbe-api}"
